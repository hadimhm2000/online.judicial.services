# پیاده‌سازی قابلیت ثبت لایحه با شماره بایگانی

## خلاصه تغییرات

### 1. پشتیبانی از شماره بایگانی در ثبت لایحه

#### فایل‌های تغییر یافته:
- `branches.py`: ذخیره کد شعبه هنگام انتخاب از لیست
- `lavayeh_handlers.py`: جستجوی کد شعبه برای ورودی دستی نام شعبه
- `lavayeh_scenario.py`: اضافه کردن منطق ثبت بر اساس شماره بایگانی

#### جریان کار:
1. کاربر روش "شماره بایگانی" را انتخاب می‌کند
2. شماره بایگانی را وارد می‌کند (اعتبارسنجی شده)
3. نام شعبه را به دو روش وارد می‌کند:
   - انتخاب از لیست (کد شعبه خودکار ذخیره می‌شود)
   - وارد کردن دستی (کد شعبه از دیتابیس جستجو می‌شود)
4. استان را انتخاب می‌کند

#### در سامانه ثنا:
- رادیو باتن `rdbCaseInfo2` (value="2") انتخاب می‌شود
- شماره بایگانی در فیلد `#txtCaseArchiveNo` وارد می‌شود
- کد شعبه در فیلد `#txtCaseHearingUnitCode` وارد می‌شود
- دکمه `#btnAddHst2` برای صحت‌سنجی کلیک می‌شود

### 2. اصلاح شخصیت وکیل در لایحه

#### تغییر:
هنگام انتخاب شخصیت "وکیل" (به جز در اعلام وکالت)، سیستم از رادیو باتن `value="6"` استفاده می‌کند.

#### فایل تغییر یافته:
- `lavayeh_scenario.py`

#### کد JavaScript اجرا شده:
```javascript
const rdb = document.querySelector('input[type="radio"][name="personType"][value="6"]#rdb6');
if (rdb) rdb.click();
```

### 3. بهبود کیبورد اظهارنامه

#### تغییر:
کیبورد انتخاب نوع شخصیت اظهارکننده اکنون **همیشه** دکمه "✅ اتمام و ادامه" را نشان می‌دهد (حتی برای اولین اظهارکننده).

#### فایل تغییر یافته:
- `keyboards.py`: تابع `create_ezhhar_declarant_person_type_kb()`

#### دلیل:
کاربران باید بتوانند بدون اضافه کردن چندین شخص، مستقیماً به مرحله بعد بروند.

### 4. ساده‌سازی فرآیند مدرک نمایندگی

#### تغییر:
برای شخص حقوقی در اظهارنامه، سیستم مستقیماً از کاربر تصویر مدرک نمایندگی را می‌خواهد **بدون نیاز به وارد کردن عنوان**.

#### فایل‌های تغییر یافته:
- `ezhharnameh_handlers.py`: حذف مرحله درخواست عنوان
- `states.py`: اضافه کردن `ezhhar_attachment_images`

#### جریان جدید:
1. پس از تکمیل متن اظهارنامه، اگر شخص حقوقی وجود داشته باشد:
2. سیستم مستقیماً می‌گوید "تصویر مدرک نمایندگی را ارسال کنید"
3. عنوان خودکار به "مدرک نمایندگی" تنظیم می‌شود
4. State به `ezhhar_attachment_images` تغییر می‌کند

### 5. اضافه کردن گزینه‌های پس از ارسال تصویر

#### تغییر:
پس از ارسال تصویر مدرک نمایندگی، کاربر سه گزینه دارد:
- ✅ اتمام ارسال تصاویر
- ➕ افزودن مدرک دیگر
- 🗑 حذف تصویر

#### فایل تغییر یافته:
- `ezhharnameh_handlers.py`: اضافه کردن handler های مخصوص `Form.ezhhar_attachment_images`

#### handler های جدید:
- `ezhhar_receive_proxy_image()`: دریافت تصاویر
- `ezhhar_finish_proxy_images()`: اتمام ارسال
- `ezhhar_add_more_after_proxy()`: افزودن مدرک دیگر
- `ezhhar_proxy_delete_image()`: حذف تصویر
- `ezhhar_proxy_images_text()`: پردازش متن (شماره برای حذف)

## فایل‌های اصلی تغییر یافته

1. **branches.py**: ذخیره کد شعبه
2. **lavayeh_handlers.py**: جستجوی کد شعبه، نمایش در پیش‌نمایش
3. **lavayeh_scenario.py**: 
   - اضافه کردن متغیرهای tracking_method, archive_number, branch_code
   - اضافه کردن منطق شرطی برای شماره بایگانی vs شماره پرونده
   - تابع `_click_validate_with_retry_archive()`
   - تغییر در پردازش شخصیت وکیل
4. **keyboards.py**: تابع `create_ezhhar_declarant_person_type_kb()` همیشه دکمه اتمام را نشان می‌دهد
5. **ezhharnameh_handlers.py**: 
   - حذف مرحله درخواست عنوان برای مدرک نمایندگی
   - اضافه کردن handler های تصویر مدرک نمایندگی
6. **states.py**: اضافه کردن `ezhhar_attachment_images`

## نکات مهم

### اعتبارسنجی شماره بایگانی:
- دو رقم اول 00-07: باید 7 رقمی باشد
- دو رقم اول 93-99: باید 6 رقمی باشد

### صحت‌سنجی در سامانه:
- برای شماره پرونده: دکمه `#btnAddHst1`
- برای شماره بایگانی: دکمه `#btnAddHst2`

### کد شعبه:
- از فیلد `Code` در داده‌های واحدها استخراج می‌شود
- در پیش‌نمایش لایحه نمایش داده می‌شود
- در سامانه وارد می‌شود: `#txtCaseHearingUnitCode`

## تست‌های پیشنهادی

1. ✅ ثبت لایحه با شماره بایگانی + انتخاب شعبه از لیست
2. ✅ ثبت لایحه با شماره بایگانی + وارد کردن دستی نام شعبه
3. ✅ ثبت لایحه با شخصیت وکیل (باید رادیو value=6 انتخاب شود)
4. ✅ ثبت اظهارنامه با شخص حقوقی (بدون نیاز به عنوان مدرک)
5. ✅ گزینه‌های بعد از ارسال تصویر مدرک نمایندگی
6. ✅ کیبورد اظهارکننده همیشه دکمه "اتمام" را نشان می‌دهد
