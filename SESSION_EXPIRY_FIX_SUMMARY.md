# اصلاح تشخیص و مدیریت انقضای نشست (منضمات + رفتار Resume)

## مشکل اصلی
تابع مرکزی تشخیص انقضای نشست ثنا — `check_and_handle_expiry` در `browser_helpers.py` —
فقط داخل چهار تابع کمکی صدا زده می‌شد: `safe_click_by_text`, `safe_type`,
`resilient_sleep`, `goto_url_with_retry`.

توابع آپلود پیوست («منضمات») در هر سه سناریو مستقیماً از `page.evaluate` و
`asyncio.sleep` خام استفاده می‌کردند و **هیچ‌وقت** این تابع را صدا نمی‌زدند؛
برای همین اگر پاپ‌آپ انقضا دقیقاً در همان لحظه (مثلاً حین آپلود فایل یا کلیک
«ذخیره») ظاهر می‌شد، هیچ‌کس آن را نمی‌دید.

علاوه بر این، رفتار قبلی حتی در جاهایی که «کار می‌کرد» هم دقیقاً چیزی که خواسته
شده بود نبود: با `raise` شدن یک Exception عمومی، کل تسک (`process_lavayeh_task`
و معادل‌هایش) از ابتدا — از رفتن به منوی اصلی — دوباره اجرا می‌شد؛ نه از همان
مرحله، و بدون این‌که دکمه‌ی «بستن» پاپ‌آپ زده شود.

## چه چیزی تغییر کرد

### ۱) `browser_helpers.py` (هسته‌ی مرکزی — تأثیر روی کل پروژه)
- **`dismiss_expiry_popup(page)`** (جدید): بعد از لاگین مجدد مدیر، دکمه‌ی
  «بستن» پاپ‌آپ خطا را روی همان صفحه‌ای که خطا رویش ظاهر شده پیدا کرده و می‌زند.
- **`handle_session_expired(bot, user_id, page=None)`**: حالا پارامتر `page`
  می‌گیرد و بعد از این‌که مدیر لاگین کرد و تب لاگین بسته شد، `dismiss_expiry_popup`
  را روی صفحه‌ی اصلی صدا می‌زند.
- **`check_and_handle_expiry(...)`**: دیگر Exception raise نمی‌کند؛ به‌جایش
  `True`/`False` برمی‌گرداند. این یعنی فراخوان‌کننده می‌تواند **همان مرحله**
  را دوباره امتحان کند به‌جای این‌که کل تسک ری‌استارت شود.
- **`safe_click_by_text` / `safe_type`**: بعد از تمدید نشست، همان کلیک/تایپ
  را از نو امتحان می‌کنند (`continue` روی همان attempt).
- **`goto_url_with_retry`**: بعد از تمدید نشست، همان URL را دوباره لود می‌کند.
- **`resilient_sleep`**: از قبل ساختارش برای این حالت آماده بود؛ فقط چون
  Exception راه می‌افتاد هیچ‌وقت به مسیر موفق نمی‌رسید — الان به‌درستی کار می‌کند.

> این تغییرات چون در هسته‌ی مشترک هستند، به‌صورت خودکار روی **همه‌ی نقاطی**
> که از `safe_click_by_text` / `safe_type` / `resilient_sleep` / `goto_url_with_retry`
> استفاده می‌کنند اثر می‌گذارند — یعنی رفتار «resume از همان مرحله» الان در
> کل فرآیند ثبت (نه فقط منضمات) برقرار است.

### ۲) رفع نقطه‌کور اصلی: توابع آپلود پیوست (منضمات)
در هر سه فایل سناریو، این تغییرات اعمال شد:

- **`_click_step_box`**: وقتی کلیک روی جعبه‌ی مرحله (مثل «منضمات») از مسیر
  مستقیم (بدون عبور از `safe_click_by_text`) انجام می‌شود، حالا صریحاً چک
  انقضا هم انجام می‌شود.
- **`lavayeh_scenario.py`**: `_upload_single_attachment_group`,
  `_click_save_doc_with_retry`, `_wait_for_upload_alerts`,
  `_delete_uploaded_files`, `_click_apply_all_with_retry` — همگی الان
  قبل/بعد از کلیک‌های حساس (ذخیره، آپلود همه، اعمال همه) و در طول
  انتظارهای طولانی (polling) چک انقضا انجام می‌دهند.
- **`ealam_vakalaht_scenario.py`**: `_upload_electronic_vakalaht`,
  `_upload_other_attachment` — همان الگو.
- **`ezhharnameh_scenario.py`**: `_upload_proxy_document`,
  `_upload_electronic_vakalaht`, `_upload_other_attachment` — همان الگو.

## رفتار جدید (سناریوی کامل)
1. مرورگر مشغول کاری در «منضمات» (یا هر مرحله‌ی دیگر) است.
2. پاپ‌آپ «اعتبار ورود قبلی منقضی شده» ظاهر می‌شود.
3. `check_and_handle_expiry` این را **همان لحظه** تشخیص می‌دهد (چه در
   safe_click/safe_type، چه در توابع آپلود پیوست).
4. ربات به مدیر پیام می‌دهد و یک **تب جدید** برای لاگین باز می‌کند (تب اصلی
   دست‌نخورده می‌ماند تا وضعیت مرحله از دست نرود).
5. مدیر لاگین می‌کند و دکمه‌ی تایید را در ربات می‌زند.
6. ربات تب لاگین را می‌بندد، برمی‌گردد به تب اصلی و **دکمه‌ی «بستن»** پاپ‌آپ را
   خودش می‌زند.
7. دقیقاً از همان مرحله (همان کلیک/تایپ/آپلودی که در حال انجامش بود) ادامه
   می‌دهد — نه از ابتدای فرآیند ثبت.

## نکات مهم برای تست
- این محیط توسعه امکان اجرای واقعی Playwright/مرورگر یا اتصال به
  `sakha2.adliran.ir` را ندارد؛ تغییرات از نظر سینتکسی با `python -m py_compile`
  تست شدند اما رفتار زمان‌اجرا (runtime) روی سامانه‌ی واقعی تست نشده است.
- پیشنهاد می‌شود قبل از استفاده‌ی production، یک بار عمداً نشست را در حین
  آپلود پیوست منقضی کنید (یا `is_expired` را موقتاً force کنید) و کل چرخه
  (تشخیص → تب جدید → لاگین → بستن پاپ‌آپ → ادامه‌ی همان مرحله) را با چشم ببینید.
- چون `check_and_handle_expiry` دیگر Exception راه نمی‌اندازد، بلوک‌های
  `except Exception as e: if isinstance(e, NavigationResetError) or "Session expired" in str(e): raise e`
  که در چند جای کد وجود دارند دیگر برای رشته‌ی «Session expired» فعال نمی‌شوند
  (چون آن رشته دیگر raise نمی‌شود) — این بی‌خطر است و کد آن‌ها دست‌نخورده
  باقی مانده چون هنوز برای `NavigationResetError` (باگ GetLegalPersonType و
  عنصر پیدا نشدن) لازم است.
