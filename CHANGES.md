# تغییرات و اصلاحات اعمال شده

## ۱. اصلاح بخش جستجوی استعلام شماره همراه

### مشکل:
وقتی سامانه پیام خطای زیر را می‌داد، بات آن را تشخیص نمی‌داد:
```
شماره همراه « 09132732495 » در پایگاه داده ثنا بنام هیچ شخص حقیقی یا حقوقی ثبت نشده است .
```

### راه‌حل:
- یک بررسی برای تشخیص پیام‌های `alert-info` و `alert-dismissable` اضافه شد
- اگر پیامی حاوی عبارت "پایگاه داده ثنا" و "ثبت نشده است" باشد:
  - متن کامل پیام به کاربر ارسال می‌شود
  - فرآیند متوقف می‌شود
  - لاگ مناسب ثبت می‌شود

**فایل تغییر یافته:** `scenarios.py` (خطوط ~158-178)

---

## ۲. اصلاح بخش اعلام وکالت - افزودن وکیل

### مشکل:
خطای زیر در لاگ ظاهر می‌شد:
```
WARNING - [EALAM] فیلد کدملی وکیل پیدا نشد. 
اینپوت‌های قابل‌مشاهده روی صفحه: ['txtBillNo', 'txtShParvaneh', 'txtNationalityCode', ...]
```

این نشان می‌داد که:
- کد ملی باید در فیلد `txtNationalityCode` وارد شود (نه `txtRealIrNationalityCode`)
- دکمه استعلام باید با `ng-click="actions.getLawyerDataWithSana"` کلیک شود
- بعد از استعلام موفق، باید دکمه "ثبت موقت" کلیک شود

### راه‌حل:

#### ۲.۱. تغییر فیلد ورود کد ملی
- سلکتور `#txtNationalityCode` به اولویت اول اضافه شد
- فیلدهای قبلی به عنوان fallback نگه داشته شدند

#### ۲.۲. اصلاح دکمه استعلام
- `ng-click` از `actions.callNationalityCode` به `actions.getLawyerDataWithSana` تغییر کرد
- پشتیبانی از جستجوی دکمه با `tooltip="استعلام ثنا"` اضافه شد
- پشتیبانی از آیکون `.glyphicon-refresh` اضافه شد

#### ۲.۳. افزودن کلیک خودکار دکمه "ثبت موقت"
تابع جدید `_click_add_lawyer_save` اضافه شد که:
- بعد از استعلام موفق از ثنا
- دکمه "ثبت موقت" یا "افزودن" را پیدا و کلیک می‌کند
- با سلکتورهای مختلف (`#btnSave`, `ng-click*="setJSSBillData"`, متن دکمه) کار می‌کند

#### ۲.۴. فرآیند کامل:
1. کلیک "افزودن" در بخش ارائه‌کننده لایحه
2. وارد کردن کد ملی در `txtNationalityCode`
3. کلیک دکمه استعلام ثنا (refresh icon)
4. منتظر ماندن برای دریافت داده‌ها از ثنا
5. کلیک خودکار دکمه "ثبت موقت"
6. تکرار برای وکلای بعدی
7. رفتن به بخش "متن"

**فایل‌های تغییر یافته:**
- `ealam_vakalaht_scenario.py` - تابع `_add_lawyer_person` (خطوط ~484-548)
- `ealam_vakalaht_scenario.py` - تابع `_click_sana_query` (خطوط ~524-598)
- `ealam_vakalaht_scenario.py` - تابع جدید `_click_add_lawyer_save` (خطوط ~601-628)

---

## ۳. بهبودهای عمومی

### لاگینگ بهتر
- پیام‌های لاگ واضح‌تر برای دیباگ
- ارسال اطلاعات دقیق‌تر به ادمین در صورت بروز خطا

### مدیریت خطا
- بررسی دقیق‌تر popup های خطا
- مدیریت حالت‌هایی که فیلدها پیدا نمی‌شوند

### پایداری
- افزایش زمان انتظار برای بارگذاری عناصر
- تلاش‌های مجدد بیشتر برای عملیات‌های حساس

---

## نحوه استفاده

پس از اعمال این تغییرات:

1. **جستجوی شماره همراه:**
   - اگر شماره در ثنا ثبت نباشد، پیام کامل سامانه به کاربر نمایش داده می‌شود
   - فرآیند به‌طور خودکار متوقف می‌شود

2. **اعلام وکالت:**
   - وارد کردن لیست کدملی وکلا در بات تلگرام
   - بات به‌طور خودکار:
     - هر وکیل را اضافه می‌کند
     - استعلام ثنا می‌گیرد
     - ثبت موقت می‌کند
     - به وکیل بعدی می‌رود
   - در نهایت به بخش "متن" می‌رود

---

## یادداشت‌های فنی

### چالش‌های حل شده:
1. **تشخیص دینامیک فیلدها:** با استفاده از چندین سلکتور به ترتیب اولویت
2. **مدیریت AngularJS:** استفاده از `ng-click`, `ng-disabled`, `ng-model` برای تعامل
3. **انتظار برای عملیات async:** استفاده از `ExtractedFromSana` برای تشخیص تکمیل استعلام
4. **Fallback strategy:** اگر روش اول کار نکرد، روش‌های جایگزین امتحان می‌شوند

### وابستگی‌ها:
- Playwright (اتوماسیون مرورگر)
- aiogram (بات تلگرام)
- Python 3.8+

---

تاریخ اعمال: ۲۷ جولای ۲۰۲۶ (۶ مرداد ۱۴۰۵)
