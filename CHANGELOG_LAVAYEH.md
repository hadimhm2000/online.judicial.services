# تغییرات بخش لایحه - نسخه ۲.۰

## تاریخ: ۱۴۰۳/۰۵/۰۶

## خلاصه تغییرات

افزودن قابلیت ثبت لایحه با دو روش متفاوت:
1. **روش سنتی**: شماره پرونده + ردیف فرعی (قابلیت قبلی که حفظ شده)
2. **روش جدید**: شماره بایگانی + نام شعبه (قابلیت جدید)

---

## تغییرات جزئی

### ۱. فایل `states.py`

**State‌های جدید اضافه شده:**
```python
lavayeh_tracking_method = State()   # انتخاب روش ثبت
lavayeh_archive_number = State()    # دریافت شماره بایگانی
lavayeh_branch_name = State()       # دریافت نام شعبه
```

**دلیل تغییر:**
برای مدیریت جریان مکالمه در دو مسیر مختلف، state‌های جدید لازم بودند.

---

### ۲. فایل `keyboards.py`

**کیبورد جدید:**
```python
lavayeh_tracking_method_kb = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(text="1️⃣ شماره پرونده و ردیف فرعی")],
        [KeyboardButton(text="2️⃣ شعبه رسیدگی کننده و شماره بایگانی")],
        [KeyboardButton(text="🔙 بازگشت")]
    ],
    resize_keyboard=True
)
```

**دلیل تغییر:**
برای نمایش دو گزینه انتخابی به کاربر پس از انتخاب عنوان لایحه.

---

### ۳. فایل `lavayeh_handlers.py`

#### ۳.۱. تابع جدید: `validate_archive_number()`

**عملکرد:**
اعتبارسنجی شماره بایگانی با قوانین زیر:
- دو رقم اول ۰۰-۰۷ → باید ۷ رقمی باشد
- دو رقم اول ۹۳-۹۹ → باید ۶ رقمی باشد

**مثال:**
```python
# معتبر
validate_archive_number("0012345")  # True, "0012345"
validate_archive_number("931234")   # True, "931234"

# نامعتبر
validate_archive_number("001234")   # False, "خطا: باید ۷ رقمی باشد"
validate_archive_number("9312345")  # False, "خطا: باید ۶ رقمی باشد"
```

#### ۳.۲. هندلر جدید: `lavayeh_get_tracking_method()`

**مسیر:** بعد از انتخاب عنوان لایحه

**عملکرد:**
- نمایش دو گزینه به کاربر
- هدایت کاربر به مسیر مناسب بر اساس انتخاب
- ذخیره روش انتخابی در state

#### ۳.۳. هندلر جدید: `lavayeh_get_archive_number()`

**مسیر:** بعد از انتخاب "شعبه رسیدگی کننده و شماره بایگانی"

**عملکرد:**
- دریافت شماره بایگانی از کاربر
- اعتبارسنجی طبق قوانین
- ذخیره در state و هدایت به دریافت نام شعبه

#### ۳.۴. هندلر جدید: `lavayeh_get_branch_name()`

**مسیر:** بعد از تایید شماره بایگانی

**عملکرد:**
- دریافت نام شعبه از کاربر
- ذخیره در state و هدایت به انتخاب استان

#### ۳.۵. به‌روزرسانی هندلر: `lavayeh_get_title()`

**قبل:**
```python
await message.answer(
    "🔢 لطفاً **شماره پرونده** را ارسال فرمایید..."
)
await state.set_state(Form.lavayeh_tracking_code)
```

**بعد:**
```python
await message.answer(
    "🔢 لطفاً روش ثبت شماره پرونده را انتخاب فرمایید:",
    reply_markup=lavayeh_tracking_method_kb
)
await state.set_state(Form.lavayeh_tracking_method)
```

#### ۳.۶. به‌روزرسانی هندلر: `lavayeh_get_province()`

**تغییر اصلی:**
- پشتیبانی از هر دو مسیر
- اگر روش "شماره بایگانی" انتخاب شده → بدون درخواست ردیف فرعی، مستقیماً به بخش اشخاص می‌رود
- اگر روش "شماره پرونده" انتخاب شده → درخواست ردیف فرعی (مانند قبل)

**کد:**
```python
tracking_method = data.get("tracking_method", "case_number")

if tracking_method == "archive_number":
    # رفتن مستقیم به بخش اشخاص
    await state.set_state(Form.lavayeh_person_type)
else:
    # درخواست ردیف فرعی
    await state.set_state(Form.lavayeh_row_number)
```

#### ۳.۷. به‌روزرسانی تابع: `build_preview()`

**تغییر اصلی:**
نمایش اطلاعات متفاوت بر اساس روش انتخابی

**روش شماره پرونده:**
```
🔢 شماره پرونده: `14001234567890123۴`
🏙 استان: **تهران**
🔢 ردیف فرعی: **1**
```

**روش شماره بایگانی:**
```
🔢 شماره بایگانی: `0012345`
🏛 نام شعبه: **شعبه ۱۰۱ دادگاه عمومی تهران**
🏙 استان: **تهران**
```

#### ۳.۸. به‌روزرسانی تابع: `_build_ealam_preview()`

**تغییر:**
همانند `build_preview()`، نمایش اطلاعات متفاوت برای بخش اعلام وکالت.

---

## متغیرهای جدید در State Data

هنگام ثبت لایحه، متغیرهای زیر در `state.data` ذخیره می‌شوند:

### مسیر شماره پرونده:
```python
{
    "tracking_method": "case_number",
    "lavayeh_tracking_code": "140012345678901234",
    "lavayeh_province": "تهران",
    "lavayeh_row_number": 1,
    # ... سایر فیلدها
}
```

### مسیر شماره بایگانی:
```python
{
    "tracking_method": "archive_number",
    "lavayeh_archive_number": "0012345",
    "lavayeh_branch_name": "شعبه ۱۰۱ دادگاه عمومی تهران",
    "lavayeh_province": "تهران",
    # ... سایر فیلدها
}
```

---

## تست‌ها

تست‌های واحد برای توابع validation در فایل `test_lavayeh_validation.py` اضافه شده‌اند.

**اجرا:**
```bash
python test_lavayeh_validation.py
```

**پوشش تست:**
- ✅ شماره پرونده ۱۸ رقمی (سال ۱۴۰۰-۱۴۰۷)
- ✅ شماره پرونده ۱۶ رقمی (سال ۱۳۹۹ و قبل)
- ✅ شماره بایگانی ۷ رقمی (دو رقم اول ۰۰-۰۷)
- ✅ شماره بایگانی ۶ رقمی (دو رقم اول ۹۳-۹۹)
- ✅ خطاهای validation برای شماره‌های نامعتبر

---

## سازگاری با نسخه قبل

✅ **تمام قابلیت‌های قبلی حفظ شده‌اند**

کاربرانی که از روش قبلی (شماره پرونده + ردیف فرعی) استفاده می‌کنند، هیچ تغییری در تجربه کاری خود نخواهند داشت.

---

## مستندات

فایل‌های مستندات اضافه شده:
- ✅ `README_LAVAYEH_UPDATE.md` - توضیحات کامل تغییرات
- ✅ `INSTALLATION.md` - راهنمای نصب و اجرا
- ✅ `CHANGELOG_LAVAYEH.md` - این فایل
- ✅ `.env.telegram.example` - نمونه فایل تنظیمات
- ✅ `test_lavayeh_validation.py` - تست‌های واحد

---

## نکات مهم برای توسعه‌دهندگان

### ۱. اضافه کردن validation جدید
اگر می‌خواهید قوانین جدیدی به شماره بایگانی اضافه کنید:
- تابع `validate_archive_number()` در `lavayeh_handlers.py` را ویرایش کنید
- تست‌های مربوطه را در `test_lavayeh_validation.py` اضافه کنید

### ۲. تغییر جریان مکالمه
برای تغییر ترتیب یا اضافه کردن مراحل جدید:
- ابتدا State جدید را در `states.py` تعریف کنید
- سپس هندلر مربوطه را در `lavayeh_handlers.py` اضافه کنید
- کیبورد لازم را در `keyboards.py` ایجاد کنید

### ۳. پیش‌نمایش
برای نمایش فیلدهای جدید در پیش‌نمایش:
- تابع `build_preview()` را در `lavayeh_handlers.py` ویرایش کنید
- اگر برای اعلام وکالت است، `_build_ealam_preview()` را نیز ویرایش کنید

---

## مشارکت‌کنندگان

- توسعه قابلیت جدید: AI Assistant
- بررسی و تست: تیم توسعه
- مستندنویسی: AI Assistant

---

## پشتیبانی

در صورت مواجه شدن با مشکل:
1. ابتدا فایل `INSTALLATION.md` را مطالعه کنید
2. تست‌های واحد را اجرا کنید: `python test_lavayeh_validation.py`
3. لاگ‌های ربات را بررسی کنید
4. Issue جدید در GitHub ایجاد کنید

---

**نسخه:** 2.0  
**تاریخ انتشار:** ۱۴۰۳/۰۵/۰۶  
**وضعیت:** ✅ تایید شده و آماده استفاده
