# دستورالعمل استقرار رفع خطا

## 📋 لیست تغییرات

### فایل‌های تغییر یافته
- ✅ `lavayeh_handlers.py` - خط 411 (حذف ReplyKeyboardRemove از import)

### فایل‌های جدید (مستندات)
- 📄 `FIX_SUMMARY.md` - خلاصه رفع خطا به فارسی
- 📄 `README_FIX.md` - راهنمای کامل رفع خطا
- 📄 `BUGFIX_GUIDE.md` - راهنما به انگلیسی
- 📄 `IMPORT_FIX_DIAGRAM.md` - نمودارها و تصاویر
- 📄 `DEPLOYMENT_INSTRUCTIONS.md` - این فایل
- 📄 `test_import_fix.py` - اسکریپت تست

## 🚀 مراحل استقرار

### مرحله 1: دریافت تغییرات

اگر از Git استفاده می‌کنید:
```bash
# دریافت آخرین تغییرات
git pull origin main

# یا اگر خودتان تغییرات را اعمال می‌کنید:
git fetch
git checkout main
```

### مرحله 2: بررسی تغییرات

```bash
# مشاهده تغییرات در lavayeh_handlers.py
git diff lavayeh_handlers.py

# یا مشاهده دستی
cat lavayeh_handlers.py | grep -A 3 -B 3 "line 411"
```

### مرحله 3: اعمال تغییرات دستی (در صورت نیاز)

اگر Git ندارید، فایل را دستی ویرایش کنید:

1. فایل `lavayeh_handlers.py` را باز کنید
2. به خط 411 بروید
3. خط را از این:
   ```python
   from keyboards import lavayeh_branch_input_method_kb, back_only_kb, ReplyKeyboardRemove
   ```
   به این تغییر دهید:
   ```python
   from keyboards import lavayeh_branch_input_method_kb, back_only_kb
   ```
4. فایل را ذخیره کنید

### مرحله 4: تست تغییرات

```bash
# ۱. تست import ها
python test_import_fix.py

# ۲. تست سیستم شعب
python test_branches_system.py

# ۳. تست کامل ربات (اختیاری)
python bot.py &
# سپس در تلگرام تست کنید و ربات را متوقف کنید
kill %1
```

### مرحله 5: اجرای ربات در محیط تولید

```bash
# فعال‌سازی محیط مجازی
source venv/bin/activate  # Linux/Mac
# یا
venv\Scripts\activate  # Windows

# اجرای ربات
python bot.py
```

### مرحله 6: تست در تلگرام

1. به ربات در تلگرام وارد شوید
2. `/start` را ارسال کنید
3. گزینه **"📝 ثبت لایحه"** را انتخاب کنید
4. مراحل را طی کنید:
   - عنوان لایحه: یکی از گزینه‌ها را انتخاب کنید
   - شماره بایگانی: یک شماره معتبر وارد کنید
   - روش انتخاب شعبه: **"🔍 انتخاب شعبه از لیست"** را انتخاب کنید
5. بررسی کنید که:
   - ✅ خطای ImportError نمایش داده نمی‌شود
   - ✅ کیبورد معمولی حذف می‌شود
   - ✅ لیست شعب به صورت inline keyboard نمایش داده می‌شود
   - ✅ می‌توانید در ساختار درختی جستجو کنید

## ✅ معیارهای موفقیت

### تست‌های پایه
- [ ] اجرای `python bot.py` بدون خطا
- [ ] اجرای `python test_import_fix.py` موفق
- [ ] اجرای `python test_branches_system.py` موفق

### تست‌های عملکردی
- [ ] انتخاب "ثبت لایحه" کار می‌کند
- [ ] ورود شماره بایگانی کار می‌کند
- [ ] انتخاب "انتخاب شعبه از لیست" بدون خطا
- [ ] نمایش لیست شعب
- [ ] جستجو در ساختار درختی شعب
- [ ] انتخاب شعبه نهایی و ادامه فرآیند

### تست‌های پایداری
- [ ] ربات بدون crash اجرا می‌شود
- [ ] حافظه مصرفی عادی است
- [ ] لاگ‌ها سالم هستند

## 🔄 بازگشت به نسخه قبل (Rollback)

در صورت بروز مشکل:

```bash
# بازگشت به نسخه قبل
git revert HEAD

# یا بازیابی فایل خاص
git checkout HEAD~1 lavayeh_handlers.py

# اجرای مجدد ربات
python bot.py
```

## 📊 مانیتورینگ

بعد از استقرار، موارد زیر را بررسی کنید:

### لاگ‌های سیستم
```bash
# مشاهده لاگ‌های ربات
tail -f bot.log

# جستجوی خطاها
grep -i error bot.log
grep -i import bot.log
```

### متریک‌های عملکرد
- تعداد کاربرانی که از "انتخاب شعبه از لیست" استفاده می‌کنند
- نرخ موفقیت انتخاب شعبه
- زمان پاسخ‌دهی سیستم

## 🐛 عیب‌یابی مشکلات احتمالی

### مشکل: هنوز خطای ImportError نمایش داده می‌شود

**راه حل:**
```bash
# بررسی محتوای فایل
cat lavayeh_handlers.py | grep -n "ReplyKeyboardRemove"

# باید فقط خط 14 را نشان دهد، نه خط 411
# اگر خط 411 هنوز ReplyKeyboardRemove دارد، تغییرات اعمال نشده است
```

### مشکل: لیست شعب خالی است

**راه حل:**
```bash
# بررسی فایل داده شعب
ls -lh units_compact.json

# بررسی صحت JSON
python -m json.tool units_compact.json > /dev/null

# تست بارگذاری داده
python -c "from branches import UNITS_DATA; print(f'تعداد واحدها: {len(UNITS_DATA)}')"
```

### مشکل: ربات پاسخ نمی‌دهد

**راه حل:**
```bash
# بررسی وضعیت ربات
ps aux | grep "python bot.py"

# بررسی اتصال شبکه
ping api.telegram.org

# بررسی توکن ربات
cat .env | grep BOT_TOKEN
```

## 📝 چک‌لیست نهایی

قبل از تکمیل استقرار، این موارد را بررسی کنید:

### قبل از استقرار
- [ ] پشتیبان از نسخه فعلی گرفته شد
- [ ] تغییرات در محیط توسعه تست شدند
- [ ] مستندات به‌روز شدند
- [ ] تیم از تغییرات مطلع شدند

### حین استقرار
- [ ] ربات متوقف شد
- [ ] تغییرات اعمال شدند
- [ ] تست‌ها اجرا شدند و موفق بودند
- [ ] ربات مجدداً راه‌اندازی شد

### بعد از استقرار
- [ ] عملکرد اصلی تست شد
- [ ] لاگ‌ها بررسی شدند
- [ ] کاربران تست کردند
- [ ] هیچ خطای جدیدی گزارش نشد

## 📞 پشتیبانی

در صورت بروز مشکل:

1. **لاگ‌ها را جمع‌آوری کنید:**
   ```bash
   # لاگ‌های ربات
   cat bot.log > bug_report.log
   
   # خطاهای سیستم
   journalctl -u telegram-bot >> bug_report.log
   ```

2. **اطلاعات محیط را جمع‌آوری کنید:**
   ```bash
   python --version >> bug_report.log
   pip list >> bug_report.log
   uname -a >> bug_report.log
   ```

3. **با تیم توسعه تماس بگیرید** و فایل `bug_report.log` را ارسال کنید

## 🎯 مراحل بعدی

بعد از استقرار موفق:

1. **مانیتورینگ:**
   - عملکرد را برای 24-48 ساعت رصد کنید
   - خطاها و هشدارها را بررسی کنید

2. **بهینه‌سازی:**
   - بازخورد کاربران را جمع‌آوری کنید
   - عملکرد سیستم را بررسی کنید

3. **مستندسازی:**
   - نتایج را مستند کنید
   - آموزش‌های لازم را به کاربران ارائه دهید

## 📄 فایل‌های مرتبط

- `FIX_SUMMARY.md` - خلاصه تغییرات
- `README_FIX.md` - راهنمای کامل
- `BUGFIX_GUIDE.md` - راهنمای فنی
- `IMPORT_FIX_DIAGRAM.md` - نمودارها

---

**نسخه:** 2.0.1  
**وضعیت:** ✅ آماده استقرار  
**اولویت:** 🔴 بالا  
**تاریخ:** امروز
