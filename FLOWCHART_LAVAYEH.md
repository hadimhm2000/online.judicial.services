# نمودار جریان بخش ثبت لایحه

## جریان کلی

```
[شروع] → [انتخاب عنوان لایحه] → [انتخاب روش ثبت] → ┬→ [مسیر ۱: شماره پرونده]
                                                          │
                                                          └→ [مسیر ۲: شماره بایگانی]
```

---

## جریان مسیر ۱: شماره پرونده (روش سنتی)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        مسیر شماره پرونده                            │
└─────────────────────────────────────────────────────────────────────┘

[انتخاب روش]
      ↓
[کاربر: "1️⃣ شماره پرونده و ردیف فرعی"]
      ↓
[ربات: لطفاً شماره پرونده را ارسال فرمایید]
      ↓
[کاربر: ورود شماره پرونده - مثلاً 140012345678901234]
      ↓
[Validation]
      ├─ سال ۱۴۰۰-۱۴۰۷ ──→ باید ۱۸ رقمی باشد ✓
      ├─ سال ۱۳۹۹ و قبل ──→ باید ۱۶ رقمی باشد ✓
      └─ غیر عددی ──→ ❌ خطا
      ↓ (در صورت موفقیت)
[ربات: لطفاً استان را انتخاب کنید]
      ↓
[کاربر: انتخاب استان - مثلاً "تهران"]
      ↓
[ربات: لطفاً ردیف فرعی را وارد کنید (۱-۳۰)]
      ↓
[کاربر: ورود ردیف فرعی - مثلاً "1"]
      ↓
[Validation: عدد بین ۱-۳۰]
      ↓ (در صورت موفقیت)
[ادامه به بخش اشخاص/وکیل]
```

---

## جریان مسیر ۲: شماره بایگانی (روش جدید)

```
┌─────────────────────────────────────────────────────────────────────┐
│                       مسیر شماره بایگانی                            │
└─────────────────────────────────────────────────────────────────────┘

[انتخاب روش]
      ↓
[کاربر: "2️⃣ شعبه رسیدگی کننده و شماره بایگانی"]
      ↓
[ربات: لطفاً شماره بایگانی را ارسال فرمایید]
│     • دو رقم اول ۰۰-۰۷ → باید ۷ رقمی باشد
│     • دو رقم اول ۹۳-۹۹ → باید ۶ رقمی باشد
      ↓
[کاربر: ورود شماره بایگانی]
      ↓
[Validation شماره بایگانی]
      ↓
      ├─ شروع با ۰۰-۰۷ ──→ بررسی ۷ رقمی ──→ ✓ یا ❌
      ├─ شروع با ۹۳-۹۹ ──→ بررسی ۶ رقمی ──→ ✓ یا ❌
      └─ سایر موارد ──→ ❌ خطا: دو رقم اول نامعتبر
      ↓ (در صورت موفقیت)
[ربات: لطفاً نام شعبه خود را تعیین کنید]
      ↓
[کاربر: ورود نام شعبه - مثلاً "شعبه ۱۰۱ دادگاه عمومی تهران"]
      ↓
[ربات: لطفاً استان را انتخاب کنید]
      ↓
[کاربر: انتخاب استان - مثلاً "تهران"]
      ↓
[⚠️ بدون درخواست ردیف فرعی!]
      ↓
[ادامه مستقیم به بخش اشخاص/وکیل]
```

---

## مقایسه دو مسیر

| مرحله | مسیر ۱: شماره پرونده | مسیر ۲: شماره بایگانی |
|-------|---------------------|----------------------|
| 1 | شماره پرونده (۱۶ یا ۱۸ رقمی) | شماره بایگانی (۶ یا ۷ رقمی) |
| 2 | استان | نام شعبه |
| 3 | ردیف فرعی (۱-۳۰) | استان |
| 4 | بخش اشخاص | بخش اشخاص |
| **تعداد ورودی‌ها** | ۳ | ۳ |
| **تفاوت اصلی** | ردیف فرعی | نام شعبه (بدون ردیف) |

---

## جزئیات Validation

### Validation شماره پرونده

```python
def validate_tracking_code(code: str):
    # بررسی عددی بودن
    if not code.isdigit():
        return False, "⚠️ فقط عدد"
    
    # استخراج سال (۴ رقم اول)
    year = int(code[:4])
    
    # قوانین
    if 1400 <= year <= 1407:
        if len(code) == 18:
            return True, code  ✓
        else:
            return False, "⚠️ باید ۱۸ رقمی باشد"  ❌
    
    elif year <= 1399:
        if len(code) == 16:
            return True, code  ✓
        else:
            return False, "⚠️ باید ۱۶ رقمی باشد"  ❌
    
    else:
        return False, "⚠️ سال نامعتبر"  ❌
```

### Validation شماره بایگانی

```python
def validate_archive_number(archive_num: str):
    # بررسی عددی بودن
    if not archive_num.isdigit():
        return False, "⚠️ فقط عدد"
    
    # بررسی حداقل طول
    if len(archive_num) < 2:
        return False, "⚠️ حداقل ۲ رقم"
    
    # استخراج دو رقم اول
    first_two = int(archive_num[:2])
    
    # قوانین
    if 0 <= first_two <= 7:      # ۰۰ تا ۰۷
        if len(archive_num) == 7:
            return True, archive_num  ✓
        else:
            return False, "⚠️ باید ۷ رقمی باشد"  ❌
    
    elif 93 <= first_two <= 99:  # ۹۳ تا ۹۹
        if len(archive_num) == 6:
            return True, archive_num  ✓
        else:
            return False, "⚠️ باید ۶ رقمی باشد"  ❌
    
    else:
        return False, "⚠️ دو رقم اول نامعتبر"  ❌
```

---

## State Flow Diagram

```
Form.lavayeh_title
      ↓
Form.lavayeh_tracking_method  ← [State جدید]
      ↓
      ├────────────────────────┬────────────────────────┐
      ↓                        ↓                        ↓
[مسیر ۱]                  [مسیر ۲]              [بازگشت]
      ↓                        ↓
Form.lavayeh_tracking_code   Form.lavayeh_archive_number  ← [State جدید]
      ↓                        ↓
Form.lavayeh_province        Form.lavayeh_branch_name      ← [State جدید]
      ↓                        ↓
Form.lavayeh_row_number      Form.lavayeh_province
      ↓                        ↓
      └────────────────────────┴────────────────────────┘
                              ↓
                   Form.lavayeh_person_type
                   (یا Form.ealam_vakalaht_national_id)
```

---

## دکمه‌های بازگشت

### در مسیر ۱:
```
[Form.lavayeh_tracking_code]
    ↓ 🔙 بازگشت
[Form.lavayeh_tracking_method]

[Form.lavayeh_province]
    ↓ 🔙 بازگشت
[Form.lavayeh_tracking_code]

[Form.lavayeh_row_number]
    ↓ 🔙 بازگشت
[Form.lavayeh_province]
```

### در مسیر ۲:
```
[Form.lavayeh_archive_number]
    ↓ 🔙 بازگشت
[Form.lavayeh_tracking_method]

[Form.lavayeh_branch_name]
    ↓ 🔙 بازگشت
[Form.lavayeh_archive_number]

[Form.lavayeh_province]
    ↓ 🔙 بازگشت
[Form.lavayeh_branch_name]
```

---

## پیش‌نمایش لایحه

### نمونه پیش‌نمایش مسیر ۱:
```
📋 پیش‌نمایش لایحه شما:

📌 عنوان: **لایحه دفاعیه**
🔢 شماره پرونده: `140012345678901234`
🏙 استان: **تهران**
🔢 ردیف فرعی: **1**

👥 اشخاص ارائه‌دهنده (۱ نفر):
  1. شخص حقیقی | کدملی: `1234567890`

📄 شرح متن:
متن لایحه ...

🖼 مدارک (۵ تصویر در ۲ عنوان):
  1. فتوکپی دادخواست — ۳ تصویر
  2. مدارک هویتی — ۲ تصویر

آیا اطلاعات فوق صحیح است؟
```

### نمونه پیش‌نمایش مسیر ۲:
```
📋 پیش‌نمایش لایحه شما:

📌 عنوان: **لایحه دفاعیه**
🔢 شماره بایگانی: `0012345`
🏛 نام شعبه: **شعبه ۱۰۱ دادگاه عمومی تهران**
🏙 استان: **تهران**

👥 اشخاص ارائه‌دهنده (۱ نفر):
  1. شخص حقیقی | کدملی: `1234567890`

📄 شرح متن:
متن لایحه ...

🖼 مدارک (۵ تصویر در ۲ عنوان):
  1. فتوکپی دادخواست — ۳ تصویر
  2. مدارک هویتی — ۲ تصویر

آیا اطلاعات فوق صحیح است؟
```

**تفاوت اصلی:** نمایش "شماره بایگانی + نام شعبه" به جای "شماره پرونده + ردیف فرعی"

---

## نکات کلیدی

1. ✅ **انعطاف‌پذیری:** کاربر می‌تواند روش دلخواه خود را انتخاب کند
2. ✅ **Validation دقیق:** هر دو روش validation کامل دارند
3. ✅ **بازگشت آسان:** در هر مرحله امکان بازگشت به مرحله قبل
4. ✅ **سازگاری کامل:** کد قدیمی همچنان کار می‌کند
5. ✅ **پیام‌های راهنما:** کاربر در هر مرحله راهنمایی می‌شود

---

## مثال‌های واقعی

### مثال ۱: ثبت با شماره پرونده

```
کاربر: 📝 ثبت لایحه
ربات: لطفاً عنوان لایحه را انتخاب کنید

کاربر: لایحه دفاعیه
ربات: لطفاً روش ثبت را انتخاب کنید:
      1️⃣ شماره پرونده و ردیف فرعی
      2️⃣ شعبه رسیدگی کننده و شماره بایگانی

کاربر: 1️⃣ شماره پرونده و ردیف فرعی
ربات: لطفاً شماره پرونده را ارسال فرمایید

کاربر: 14001234567890123۴
ربات: لطفاً استان را انتخاب کنید

کاربر: تهران
ربات: لطفاً ردیف فرعی را وارد کنید (۱-۳۰)

کاربر: 1
ربات: ✅ ثبت شد. حالا نوع شخصیت را انتخاب کنید ...
```

### مثال ۲: ثبت با شماره بایگانی

```
کاربر: 📝 ثبت لایحه
ربات: لطفاً عنوان لایحه را انتخاب کنید

کاربر: لایحه دفاعیه
ربات: لطفاً روش ثبت را انتخاب کنید:
      1️⃣ شماره پرونده و ردیف فرعی
      2️⃣ شعبه رسیدگی کننده و شماره بایگانی

کاربر: 2️⃣ شعبه رسیدگی کننده و شماره بایگانی
ربات: لطفاً شماره بایگانی را ارسال فرمایید
      • دو رقم اول ۰۰-۰۷ → باید ۷ رقمی باشد
      • دو رقم اول ۹۳-۹۹ → باید ۶ رقمی باشد

کاربر: 0012345
ربات: ✅ شماره بایگانی ثبت شد. لطفاً نام شعبه را تعیین کنید

کاربر: شعبه ۱۰۱ دادگاه عمومی تهران
ربات: لطفاً استان را انتخاب کنید

کاربر: تهران
ربات: ✅ ثبت شد. حالا نوع شخصیت را انتخاب کنید ...
```

---

**نکته:** این flowchart به صورت متنی است. برای نمایش بهتر می‌توانید از ابزارهایی مانند draw.io یا mermaid استفاده کنید.
